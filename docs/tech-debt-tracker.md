# Tech Debt & Doc Drift Tracker

> The "garbage collection" ledger for this codebase (tech debt compounds like high-interest loans — pay in small installments continuously rather than deferring to one big payoff).
> Each entry: symptom / evidence / recommended action / status. Append new drift when found; mark ✅ when resolved but keep the record.
> Initial entries from a full cross-check of `src/` + `web/` + all documentation.

## Documentation Drift (doc-rot)

| # | Symptom | Evidence | Action | Status |
|---|---|---|---|---|
| D1 | CLAUDE.md described the `src/` TS migration, React frontend, idle-sleep, multi-channel/tasks/threads **all as "PoC limitations / TODO"** | `package.json` main stack is `src/`; `agentManager.ts` has `IDLE_MS`; web fully implemented | Slim CLAUDE.md to a directory + correct current state | ✅ Fixed this session |
| D2 | CLAUDE.md "Data Model" section was partial and inaccurate (task enums, activity_log, sidebar_prefs, mentions table, lastReadSeq missing) | Compared against `schema.ts` | Redirect to `docs/generated/db-schema.md` | ✅ Fixed this session |
| D3 | CLAUDE.md Runtime table said claude "MCP tools or open-tag CLI", only listed three runtimes; "SSE is lightweight notifications only" | `claudeRuntime.ts` has no `--mcp-config`; `detectRuntimes` lists 5+; `socketio.ts` emits full message:new | Use `ARCHITECTURE.md` as source of truth | ✅ Fixed this session |
| D4 | `FEATURES.md` P4 (tasks/threads), P6 (Agent Profile seven facets/member grouping) marked `[ ]` incomplete, actually implemented | `Chat.tsx`/`Members.tsx`/`TaskBoard.tsx` + corresponding `/api/tasks`, `/api/channels/:id/threads` | **P4 corrected** (slice03 doc-sync: boxes ticked + endpoints documented + DELETE); P6 facet check pending | 🟡 P4 corrected, P6 pending |
| D5 | ~~"`DESIGN.md` is external material, not this app's design doc"~~ **Corrected (mis-judgment)**: `DESIGN.md` = **the project's own visual system standard** — a warm-editorial design system, the project's own visual language; a deliberate design decision, **not debt** | `styles.css` implements a relevant subset (design systems are normally broader than any single app's usage, which is normal) | Keep as frontend design spec; use `/impeccable:impeccable` at build/finish time to enforce craft/a11y/interaction quality per DESIGN.md | ✅ Clarified (not debt) |
| D6 | `functional-matrix.md` open-tag current-state column (✅/🟡/❌) **severely stale overall**: profile seven facets, reactions, and many human-side endpoints (create/archive channel, join/leave/read, DM, search, full-server task board, task realtime) are actually implemented, matrix still shows ❌/🟡 | Discovered stale ❌ for profile-facet + reactions while selecting work items; code audit showed they were already done | **Do not use the matrix as a work queue** (MISSION covered this); needs a one-time sweep against current `src/` + `web/`. Real remaining gaps: message edit PATCH | ⬜ Pending |

## Implementation Gaps / Internal Inconsistencies

| # | Symptom | Evidence | Action | Status |
|---|---|---|---|---|
| I1 | ~~Not a git repo~~ **Git initialized** | `git log` shows full commit history; `.gitignore` in place | ~~git init + initial commit~~ | ✅ Done |
| I2 | @mentions: backend stores structured `message_mentions`; **frontend both uses text regex (allowlist) rather than consuming the structured array** | Render side `web/src/messageRender.tsx` `processMessageContent` (slice04 replaced and removed old `mentionize()`); completion side `Chat.tsx` `onInput`; both char-sets unified in slice05 to `[\p{L}\p{N}_-]/u` | Unify: frontend highlights from `mentions[]` dropping regex, or confirm "regex + allowlist" is final state | ✅ Fixed — `processMessageContent` now resolves `@` from the message's own `mentions[]` (server-recorded, channel-scoped) instead of a workspace-wide name map; a non-member `@` renders as plain text. Pairs with Slack-style mention auto-join in `core.createMessage` (public channels). |
| I3 | `/api/messages/sync?since=` (seq incremental) endpoint exists but **web does not use it** — steady state uses socket.io full message:new + initial `GET /messages/channel/:id` | `web/src/store.tsx` | Clarify sync endpoint role (offline catch-up only?) and align in docs; evaluate before horizontal scale | ⬜ Pending |
| I5 | Architecture invariants (file ≤1000 lines, cross-plane import direction, boundary parse-don't-validate) **are only prose, not mechanically enforced** | No corresponding lint/CI | Add custom lint + structural tests, errors include fix instructions | ⬜ Pending |
| I6 | Single-instance socket.io emits directly; horizontal scale requires `@socket.io/redis-adapter` | `realtime.ts` has TODO comment | Add redis-adapter before scaling | ⬜ Pending (not blocking) |
| I7 | **Task status enum inconsistent across three locations**: `schema.ts` comment + `docs/generated/db-schema.md` said `open/claimed/in_progress/done/cancelled`; but web TaskBoard + `prompt.ts` both use `todo/in_progress/in_review/done/closed` (claim is a separate operation) | Cross-validation across three files + `core.ts` writes `todo`/`in_progress` | ✅ Fixed: `schema.ts` comment + `db-schema.md` aligned to `todo/in_progress/in_review/done/closed`; also fixed latent bug in `routes-api.ts:666` PATCH defaulting to illegal `"open"` → changed to `"todo"` | ✅ Done |
| I8 | API surface documentation overlap risk between two files listing REST/socket endpoints | Both files list APIs | Designate one file as authoritative API surface reference, the other only tracks open-tag implementation status | ⬜ Pending |
| I9 | Dev mode messages/trajectories/AI replies **appear doubled** | React StrictMode + async socket cleanup doesn't close late-arriving connections → two sockets each receive once (`web/src/store.tsx` socket useEffect) | Add `cancelled` flag: close late-arriving connection immediately if component already unmounted | ✅ Done (commit f2b0251; browser verified single copy on send) |
| I10 | Composer **Chinese IME Enter key sends prematurely** | onKeyDown doesn't check IME composition state (`web/src/views/Chat.tsx` main composer + thread textarea, slice05 switched input→textarea, IME guard retained) | Add `e.nativeEvent.isComposing` check | ✅ Done (f2b0251; simulated composition Enter does not send, normal Enter sends) |
| I11 | ~~Message row **task pill has inline hardcoded colors**~~ | Original `Chat.tsx` task-pill `style={{...}}` | slice05 §6 extracted to `.task-pill` CSS class (`--success` 1px border + transparent background + pill, editorial style, no hardcoded colors); browser computed style verified | ✅ Done (slice05) |
| I12 | **Dual icon source**: `web/src/icons.tsx` custom SVGs coexist with `lucide-react` | `icons.tsx` still has IconWrench/IconFile/IconExternalLink/IconDownload in use; new UI defaults to lucide (slice05 composer uses ImagePlus/Paperclip/Send, message row toolbar uses Smile/MessageCircle/MoreHorizontal) | Progressively migrate icons.tsx remainder to lucide-react then delete | ⬜ Pending (progressive) |
| I13 | ~~**`styles.css` many off-scale border-radii / hardcoded colors**~~ | ~30 places with border-radius (3/5/7/9/10/14px) + scattered hardcoded colors (error red, inbox glyph blue/purple/rose tint, white #fff, mention yellow #fdf2d6, modal overlay, ref-chan hover) | slice05 tokenization sweep: border-radius snapped to DESIGN.md scale (3/5→4, 7/9→8, 10→12, 14→16); hardcoded colors promoted to `:root` sidecar tokens (`--error*` aligned with DESIGN.md semantic-error + `--tint-blue/lav/rose/green` + `--on-ink` + `--mention` + `--scrim`); `grep` verified zero hardcoded hex outside `:root`, all radii on scale. **Decision**: `box-shadow` `rgba(0,0,0/12,10,9, α)` kept as-is — these are per-element elevation conventions (not brand color drift); forcing tokens would flatten depth hierarchy; acceptable | ✅ Done (slice05; shadows kept by convention) |
| I14 | **`daemonHub` global `Set` broadcast, no serverId routing** → multiple daemons on the same server process (connected to different servers) would **cross-contaminate**: every `agent:start/deliver/stop/reset` fans out to all daemons | Original `daemonHub.ts` `Set<WebSocket>` + `broadcastToDaemons` with no filter | Changed to `Map<ws,serverId>`, `broadcastToDaemons(serverId,…)`/`requestDaemon(serverId,…)`/`daemonCount(serverId)` route by serverId | ✅ Done (this session; bare two-daemon two-server end-to-end verified: A triggers → A daemon receives agent:start×2+deliver, B gets zero) |
| I15 | ~~**`agentConfig.machineKey` hardcoded to `BOOTSTRAP_KEY`** + `auth.ts:35` `token===BOOTSTRAP_KEY` universal passthrough → agent plane backdoor~~ | — | **Fixed (slice10, per-agent independent token)**: `agentConfig` mints `sk_agent_*` stored as `agentTokenHash`, injected as `OPEN_TAG_AGENT_TOKEN`; `resolveAgent` reverse-resolves by hash, removed universal passthrough + machine key as agent credential. curl 4 assertions (old key 401 / real token 200 / cross-agent 401 / fabricated 401) + E2E (daemon spawn real agent message/check 200) verified. **Also added zombie reconciliation** (`ws.ts onReady` uses daemon `runningAgents` to reconcile: stale `active` agents set inactive after server+daemon both restart; daemon survives server restart → reports live agents → skips zero-desync. Live verified: manufactured active zombie → restart daemon → `reconciled stale-active agent → inactive` → back to inactive). **Remaining hardening** (low priority): token-file hot-rotation / per-task short-lived tokens with expiry | ✅ Done (slice10 + zombie reconciliation) |
| I16 | **`machine:status` has no `statusVersion` ordering protection** (agents may receive status events out of order) | `socketio.ts`/`ws.ts`/`store.tsx` (currently online/offline each publish once; frontend reloads machine list on event) | Add monotonic `statusVersion` + frontend drops stale versions | ⬜ Pending (low priority; current "reload on event" behavior is functionally correct) |
| I17 | **Daemon standing prompt missing several behavioral sections** | Diff against known correct standing prompt (345 lines): command model/threads/naming already aligned (slice07); **missing**: Credential hygiene (redact `sk_*_<redacted>`), Compaction safety (CRITICAL), Conversation etiquette, ~~Channel awareness~~ (✅ this session), ~~Discovering people/channels~~ (✅ this session), Formatting (Mentions/Channel Refs, non-ASCII URLs), Third-party integrations (`integration list/login/env`), `action prepare`, `inbox check` alias, `reminder log/update`, explicit Startup sequence, Splitting tasks for parallel | Fill in per confirmed ground truth (skin = our `open-tag` CLI, structure = standard behavior); credential redaction first (security) | 🟡 In progress (gaps G-H remain: Formatting/CJK-URL/historical ref/private channel confidentiality/task splitting parallel; `Channel awareness` + `Discovering people/channels` added this session in `prompt.ts` under `## Channels & people` + `### Channel awareness`, open-tag CLI skin, relies on existing `server info` capability; private/role markers deferred — depend on `server info` returning `visibility`/`role` fields, which `routes-agent.ts:190` currently only returns name/description/joined) |
| I18 | **Third-party integration login not implemented — intentionally deferred** | Agent detail Apps tab "CONNECTED APPS / Apps this agent has logged into". CLI `integration list/login/env`; endpoints `/integrations/clients`/`/integrations/marketplace`/`/integrations/agents/{id}`/`/integrations/grants/{grantId}/revoke`. Essentially: OAuth provider pattern for logging into external third-party apps (GitHub/Gitea etc.) | **Not building now**: self-hosted OSS has no real external services to connect to; building it now produces only an empty stub (violates real-verification principle). Build the specific integration when there is a real external service to connect to | 🅿️ Deferred (user decision 2026-06-17: documented, do when needed). **Do not proactively build empty stubs** |
| I19 | **Action cards (B-mode quick-commit action card)** | `action:prepare` scope declared but not implemented. Full contract extracted + live-verified. Card = message with `actionMetadata{kind:"action-card",state,action}`; agent sends via `open-tag action prepare --target <ch>` + stdin; 4 types `channel:create`/`agent:create`/`channel:add_member`/`integration:approve_agent_login`; 3 endpoints `/actions/{id}/event\|execute\|mark-executed` | **Implemented + verified** (slice09): agent-api `action/prepare` (scope action:prepare) + human `POST /api/actions/:id/mark-executed` + frontend card render / pre-filled modal (DESIGN.md skin). createMessage accepts actionMetadata (schema zero-migration, field already existed). execute-as-human security: mark-executed only marks; actual creation goes through existing /api/channels\|/api/agents (as human), no new privileged path. curl + browser end-to-end verified. **Deferred**: channel:add_member, integration:approve_agent_login (= I18), /actions/:id/event (telemetry), /execute (integration only) | ✅ Done (slice09). **Note: mention-actions (`/messages/mention-actions/execute`) is a separate mechanism from action cards; not implemented** |
| I20 | **`BOOTSTRAP_KEY` defaults to hardcoded `poc-secret-key` (machine layer connection key)** | `auth.ts:7` (`process.env.DAEMON_BOOTSTRAP_KEY ?? "poc-secret-key"`); `ws.ts` daemon WS `/daemon/connect?key=` uses it to authenticate the default workspace. **No longer an agent credential** (I15 fixed `resolveAgent` to not touch it) → risk far lower than old agent universal passthrough (machine layer has serverId isolation as backstop: even with the key, it only connects to the default workspace) | Before production deployment, harden by either: ① requiring `DAEMON_BOOTSTRAP_KEY` with no built-in default, or ② removing the bootstrap path so every machine uses a real `sk_machine_*` key | ⬜ Pending (low priority, not a backdoor; resolve before production deployment) |
| I21 | **Settings sub-page routing entirely unreachable**: `main.tsx` only had `Route path="settings"` (exact match to `/s/:server/settings`), no `settings/:section`; Settings sidebar buttons navigate to `/settings/{server\|notifications\|machines}` → fall through to `path="*"` RootRedirect → bounce to channel. Result: workspace profile/notifications/machines sub-pages **always bounced on click, never actually opened** (`Settings` component reads `useParams().section` which is always undefined → only shows account) | Discovered while implementing workspace avatar: avatar UI is in ServerSettings but `/settings/server` was unreachable | Added one line to `main.tsx`: `<Route path="settings/:section" element={<Settings />} />` (account default page retains `path="settings"`) | ✅ Fixed (this session, with workspace avatar; browser verified `/settings/server` no longer redirects, ServerSettings renders) |
| I22 | **Write endpoints have no human role permission checks**: `POST /api/channels`, `POST/PATCH/DELETE /api/agents`, agent start/stop/reset etc. **any server member can call** (member can freely create/delete channels and agents); only `PATCH /servers/:id` and avatar had manual owner/admin checks | Discovered when adding member role management: needed permissions foundation first | Created `src/server/capabilities.ts` (8 caps × owner/admin/member) + `can`/`requireCap`; integrated into create/delete channel·agent·server settings·avatar; `GET /api/servers` returns `role`+`capabilities` for frontend visibility control. **Frontend shows/hides by cap + member role management endpoints (PATCH/DELETE members) = S1/S3** | ✅ Done (P0 backend enforced: curl member→403/owner→200/all 8 caps; S1 frontend: member does not see create channel·agent buttons, browser verified) |
| I23 | **Workspace tab deep-link reload returns `x-server-id header required`** + no Refresh button | Direct page reload to `/s/:server/agent/:id?agentTab=workspace` → `GET /agents/:id/workspace-files` missing `x-server-id` header (store/sidRef not ready when request fires; on reload agent detail data also partially broken Session/Status empty); **in-SPA navigation works fine**. Also: workspace panel is missing a Refresh button (the equivalent panel in similar products has one) | ① Don't fire serverId-dependent requests before store init (sidRef ready); or request layer waits for sidRef; ② add Refresh button to workspace tab | ⬜ Pending (low priority; only triggers on deep-link reload, SPA navigation unaffected) |
| I24 | **Several polish items from UX audit intentionally deferred** | Remaining non-critical items from a full-site persona review | Deferred (not omissions): ① trajectory column collapse — cross-component `Layout`/`Chat` grid coordination, high regression risk given recent scrim grid fix, and resizer already provides a workaround; ② reactions picker + mention menu full keyboard accessibility — mention menu now supports ↑/↓ + Enter/Tab selection with a highlighted-index + Esc to close (`Chat.tsx`); reactions picker keyboard access still pending (trigger is hover, needs focus management refactor); ③ j/k message navigation — not verified in comparable products, do not invent; ④ Help onboarding tour — already have empty-state guidance (add-computer onboarding modal/empty copy), full tour is over-engineering. **High-value items done this round**: Cmd+K global jump, site-wide :focus-visible, 7 modal Esc handlers, agent status dot aria, ConfirmModal (replaces native confirm×4), rail labels, DM uuid fallback, wake-state visualization, shadow elevation token into design system | 🅿️ Deferred (do when user requests) |
| I25 | **codex runtime has no per-agent `CODEX_HOME` / MCP isolation injection** | `src/daemon/codexRuntime.ts` current comments and implementation use user default `~/.codex`; `ARCHITECTURE.md` codex section marks this as a target spec not current implementation | Future: generate per-agent `CODEX_HOME/config.toml` (0o600, includes MCP server/env) for each agent, and handle auth/plugin inheritance strategy | ⬜ Pending |
| I26 | **`agents.name` has no `UNIQUE` constraint** although it is the @mention handle (and `dm:@<name>` lookup key) — two agents on the same server can share a name, making `@<name>`/`dm:@<name>` ambiguous | `schema.ts`: `users.name` is `.notNull().unique()`, `agents.name` is only `.notNull()`; `resolveTarget` (`core.ts`) does a plain `eq(agents.name, peer)` and silently picks the first match on collision | Add a per-server `UNIQUE(serverId, name)` index on `agents` (+ drizzle migration + `docs/generated/db-schema.md`); requires a plan for any existing duplicates before the migration can apply | ✅ Fixed (this session): **partial** unique index `agents_name_uniq` `(serverId, name) WHERE deleted_at IS NULL` (`schema.ts`) — unique among live agents only, so soft-deleted names are reusable (better than a full-table `UNIQUE`, which would permanently lock a deleted name); `POST /api/agents` uses `ON CONFLICT` against the index → friendly **409**, race-proof (no SELECT-then-INSERT gap; verified 0×500 under concurrent double-create). Existing-duplicate plan (the "plan for existing duplicates" this entry asked for): `scripts/heal-dup-agent-names.ts` — dry-run report by default, `--apply` renames all-but-newest live dupe to `<name>-<id8>` so none is lost and shadowed agents become addressable, then `npm run db:push`. Verified: real curl chain (create 200 / dup 409 / delete→profile gone / recreate-after-delete 200) + raw `INSERT` blocked by the index |
| I27 | **Pre-existing agent names with spaces/illegal chars are not backfilled** | Creation-time validation (#3) only guards new agents via `POST /api/agents`; rows created earlier (e.g. an agent whose `name` is `Code-review master`) keep their space-bearing handle and stay hard to `@`/`dm:@` by text | Decide a backfill: rename offending handles (accepting that text-based historical message/DM resolution keyed on the old `name` may shift — uuid-keyed resolution is unaffected), or leave as-is. Kept out of #3 to keep the fix surgical | ⬜ Pending (follow-up to #3; intentionally deferred) |
| I28 | **No test harness → validators like `invalidAgentName` have no CI regression guard** | PR #4 review ④: the 26-case logic check for `invalidAgentName` was run by hand, not committed; the repo has no test runner (`package.json` has no `test` script, CI only typechecks + builds, `test/` is empty), so a future edit that loosens the regex/length would not fail CI | Introduce a lightweight runner (`node:test` + `tsx`) + a CI step, then land `test/agent-name.test.ts` (and similar). Repo-wide infra — related to I5 ("invariants are only prose, not mechanically enforced"); kept out of the small #3 fix | ⬜ Pending (follow-up to #3; infra decision) |
| I29 | **A message whose body is raw HTML rendered as an empty bubble** (content delivered + persisted, but invisible) | `web/src/messageRender.tsx` ran react-markdown with no `rehype-raw`; react-markdown's default silently DROPS mdast `html` nodes, so an all-HTML body (e.g. an agent posting a `<div class="head head-agent">…` component, 909 chars) produced zero visible output. Confirmed against the running app + DB: content length 909, rendered `.mbody .md` empty | Added `remarkHtmlAsText` (remark plugin) downgrading every `html` node to a literal `text` node → raw HTML shows its escaped source (React escapes on render — no XSS, no new HTML-render surface). Unit test `test/messageRender.html.unit.test.ts` + browser-verified (HTML message shows source; `**bold**` / `#channel` links unaffected) | ✅ Fixed (this session) |
| I30 | **A reply threaded onto a system message becomes UI-unreachable** | `resolveTarget` (`core.ts`) matched a `#channel:<shortid>` parent by id-prefix with **no `senderType` filter**, so a shortid landing on a system message ("X claimed / created task") created a thread off it via `getOrCreateThread`; system messages render with no "open thread" affordance → the reply is delivered + persisted but invisible. Observed live: `message send --target "#…:27abf892"` (27abf892 = a system "claimed" message) buried the reply in `thread-27abf892` | `resolveTarget` now returns `null` when the resolved parent is a system message → `/agent-api` surfaces `404 TARGET_FAILED` instead of silently burying it. Integration test `test/threadTarget.integration.ts` (real pg: real message → thread OK; system message → null + no orphan thread) | ✅ Fixed (this session) |
| I31 | **Oversized uploads are silently truncated, not rejected** | `attachments.ts` busboy `limits.fileSize: 25MB`: on overflow busboy ends the file stream early; `saveObject` persists the truncated bytes in **both** local and S3 drivers, and the upload still returns `200` (no `file.truncated`/`limit` check). A >25MB file is stored corrupt-but-looks-fine. Pre-existing, not S3-specific; surfaced while building the S3 E2E | Detect the busboy `limit` event per file → reject the upload (or mark the attachment) instead of silently storing a truncated object; covers both drivers at the one `attachments.ts` boundary | ⬜ Pending (deferred — kept the S3 close-loop diff surgical) |
| I32 | **`drizzle-kit` (even latest `0.31.10`) hard-depends on the deprecated `@esbuild-kit/esm-loader` → `@esbuild-kit/core-utils` → old `esbuild` chain (GHSA-67mh-4wv8-2f99, moderate)** — no drizzle-kit release sheds it; `npm audit fix --force` only "fixes" it by *downgrading* drizzle-kit to `0.18.1` (wrong — breaks schema push) | `package.json` → `overrides["@esbuild-kit/core-utils"].esbuild = "^0.25.0"` forces the patched esbuild into that subtree (a fresh `npm install` is required for the override to fully resolve; an incremental install leaves the nested copy `invalid`) | **Do not remove the override** or the moderate vuln returns. The advisory is esbuild's *dev-server* CORS leak, which is **not on any path we run** (drizzle-kit uses @esbuild-kit only to transpile `drizzle.config.ts`, it never starts a serve) — so it was non-exploitable even before; the override just keeps `npm audit` at 0. Drop it once drizzle-kit ships without @esbuild-kit | ✅ Done (closes #22; verified `drizzle-kit push` still loads config + applies schema after the override, full test suite + socket WS smoke green) |
| I33 | **`@fancyboi999/open-tag-daemon` published to npm + onboarding wired to npx** (was: built but unpublished + UI showed `npm run daemon`) | `packages/daemon/` (esbuild bundle of `src/daemon` + `src/cli`); `.github/workflows/publish-daemon.yml`; `web/src/views/misc.tsx` + `connectModal*` locales. | ✅ ① Published `v0.1.0` (user scope, public, provenance). Publish auth moved to npm **Trusted Publishing (OIDC, token-less)** — workflow drops `NODE_AUTH_TOKEN`, upgrades npm ≥11.5.1, relies on `id-token: write` + the npm-side trusted-publisher config (owner `fancyboi999` / repo `open-tag` / `publish-daemon.yml`). ✅ ② Connect-a-computer modal + empty-state hint now show `npx @fancyboi999/open-tag-daemon …` (#33). ⚠️ **Remaining:** revoke the first publish token (it was pasted in chat) + delete the now-unused `NPM_TOKEN` repo secret. | 🟢 Done (published + npx onboarding + Trusted Publishing); only token-revocation cleanup remains |
| I34 | **Task status mutators promoted a plain message into a numberless task (`#-`/`#null`)**, and **DM tasks shared the workspace counter** | `claimTask`/`unclaimTask`/`setTaskStatus` did an unconditional `UPDATE … SET taskStatus=…` with no `isNotNull(taskStatus)` guard and minted no number; agent `task/claim`·`task/update` resolve *any* message id → an agent claiming/updating a plain **DM** message turned it into a task with `taskNumber=null` (audit trail literally read `#null`). Separately every task number came from one per-server counter, so DM and channel tasks were indistinguishable | **Invariant `taskStatus ⟺ taskNumber`**: added `isNotNull(taskStatus)` to the three mutators (a plain message → 404, no promotion); agent claim/update auto-convert via `convertMessageToTask` first (canonical mint). **Per-DM numbering**: `taskNumberKey` routes DMs to `tasknum:dm:<channelId>`; `reconcileCounters` reconciles every scope on boot; frontend labels DM tasks `@peer` + gives DMs a `chat\|tasks` board; status popover switched to `Select.tsx`'s fixed positioning (was trapped under the next card's transform stacking context). `scripts/heal-task-numbers.ts` backfills legacy numberless rows. Verified: real HTTP agent-api chain + browser. ⚠️ **Introduced**: a bare `task #N` is now unique only within its scope — inline `task #N` resolution (`Chat.tsx navToken` → `/api/tasks/server` first-match) can mis-resolve across a DM/channel #N collision. Acceptable (DM scopes are 1:1 + the in-channel local match wins first); revisit if it bites | ✅ Fixed (this session) |
| I35 | **In a thread, `@`-mentioning a teammate who hadn't replied in that thread yet was silently dropped — mention unrecorded, agent never woken** | `core.createMessage` gated mention auto-join on `ch?.type === "channel"`, lumping `thread` with `private`/`dm` as excluded. A thread's `channelMembers` is only "who has replied so far", and `parseMentions` matches `@name` **only against that set**, so `@an-outsider` resolved to nobody → no `message_mentions` row, no wake delivery, and the frontend rendered the `@` as plain text (an `@` the server never recorded). Reproduced live in the prod DB: thread `a879e694` had `@claudecode-dev` in message bodies (seq 32/35/45) yet **empty** `message_mentions` every time; `claudecode-dev` was a member of the parent channel but never of the thread | **A thread now inherits its parent channel's @-reach** (`core.mentionAutoJoinPool`, the same parent-channel inheritance `socketio.canReadChannel` uses): public-parent thread → workspace pool, private-parent thread → parent members only, dm-parent → none; `autoJoinMentioned` takes the candidate pool as a param instead of always `workspaceMembers`, and the gate is now `ch && content.includes("@")`. Unit `test/mention.unit.test.ts` pins the security boundary (a public space pulls any teammate in; a members-only space never pulls an outsider) + integration `test/mention.integration.ts` cases [5]/[6] exercise the real thread→parent-channel path against Postgres (public-parent thread auto-joins + wakes a non-member; private-parent thread stays a no-op). The orphaned-thread fallback (parent message/channel deleted → `mentionAutoJoinPool` degrades to thread-own members) now logs a `warn` so a silently-dropped `@` stays debuggable. Real-run verified on an isolated stack: public-channel thread `@agentbravo` (non-member) → mention recorded + auto-joined + `wakeAgents:["agentbravo(@)"]`; private-channel thread `@agentbravo` (non-parent-member) → no mention, no auto-join, no wake (no leak). ARCHITECTURE.md mention-auto-join invariant rewritten | ✅ Fixed (this session) |
| I36 | **`thread` channels still slip past a few `type === "channel"`-only guards** (thread-as-channel audit, follow-up to I35) | A full sweep of `type ===` branches while fixing I35 found three spots that mishandle a thread but are **independent of the @-wake bug**: ① `routes-agent.ts` `server/info` filters `c.type !== "dm"`, so thread channels (internal `thread-xxxx` names) appear in the agent's channel list, polluting its context; ② `routes-api.ts` PATCH channel `visibility` has no `type !== "thread"` guard → a thread's `type` can be flipped to `channel`/`private`, breaking the `parentMessageId` invariant (it'd surface as a stray fake channel in the sidebar); ③ archive + user-side `join` endpoints have no thread guard (low impact — thread ids aren't user-enumerable) | **Fixed** (three surgical changes, all gated by a failing integration test `test/threadChannelGuards.integration.ts`): ① `routes-agent.ts:237` `server/info` filter adds `&& c.type !== "thread"` — thread channels never leak into agent context; ② `routes-api/channels.ts` PATCH handler fetches the channel type first and returns 403 if `type === "thread"`; ③ join and leave handlers add `if (ch.type === "thread") return 403`; archive handler adds the same guard after the `manageChannels` capability check. Regression: public channel join/archive/unarchive unaffected (still 200). All 16 checks pass (8 fail on origin/main, 0 fail after fix). Typecheck clean. | ✅ Fixed (follow-up PR to I35) |
| I37 | **Agent Live Trace panel: traces vanished on channel/DM switch + `agent_activity_log` grew unbounded + long lines forced horizontal scroll** | `web/src/views/Chat.tsx` kept `traj` in local state and wiped it (`setTraj([])`) on channel switch; `agent_activity_log` was insert-only (no retention) in `ws.ts logActivity`; `.traj` lines had no wrap and `traj-col` had `overflow:auto` | **Fixed (task #2)**: (1) lifted the trace feed into a bounded ring buffer in `store.tsx` (new `web/src/trajBuffer.ts`, cap 300, drop-oldest) so it survives channel/DM switches; (2) `logActivity` now prunes to the newest 500 rows per agent on insert (`pruneAgentActivityLog` / `ACTIVITY_LOG_CAP` in `ws.ts`); (3) CSS `.traj{overflow-wrap:anywhere}` + `traj-col{overflow-x:hidden}`. Verified: `trajBuffer` unit 6/6, prune integration 6/6 (isolated DB), CSS no-h-scroll + wrap in a real browser. **Remaining trade-offs**: (a) prune runs on *every* insert — under very high trace volume that is a delete-per-insert; candidate for batching / probabilistic pruning if it shows up under load; (b) refresh-restore deferred per @fancy — could later rehydrate from the existing `GET /api/agents/:id/activity-log`, which for the *global* feed would need a new cross-agent recent-activity endpoint | ✅ Done (task #2) |
| I38 | **UI advertised runtimes the daemon couldn't run** (gemini, opencode/kimi/pi/cursor stubs) | `detectRuntimes()` (`runtimes.ts`) probed `claude/codex/copilot/kimi/gemini/opencode` and `RUNTIMES` (`Members.tsx`) listed `gemini`/`opencode`, but `REG` only implemented `claude`/`codex`/`copilot`. Selecting an un-implemented one created an agent that immediately went `offline` with `no runtime: <x>`. Surfaced while adding copilot | **gemini purged** (Google retired the standalone Gemini CLI 2026-06-18, folded into Antigravity — removed everywhere). **opencode + kimi + pi + cursor now implemented** (`opencodeRuntime.ts` / `kimiRuntime.ts` / `piRuntime.ts` / `cursorRuntime.ts`, in `REG`). Every runtime `detectRuntimes()` probes now has an adapter | ✅ Resolved — drift closed (gemini removed; copilot/opencode/kimi/pi/cursor implemented) |
| I39 | **Copilot + kimi model lists are static, not discovered from the user's actual account** | Dynamic model discovery landed for **opencode / cursor / pi** (model list via shell command) and **claude / codex** (thinking/effort levels probed; `listModels.ts`, `runtimeModels.ts`). **copilot and kimi are still static**: copilot's only programmatic source is the `copilot --acp` `availableModels` handshake (ACP / JSON-RPC over stdio); kimi similarly requires its own JSON-RPC-over-stdio session setup — neither fits the daemon's current one-shot shell-command adapter. multica already has a complete ACP handshake sequence that could be adapted. Picking a model the account lacks still fails at runtime (stderr → `error` activity), but cannot be pre-warned | Adapt multica's ACP handshake pattern: a lightweight probe subprocess that does the ACP/JSON-RPC init, reads `availableModels`, and exits — reuse the existing `probe-models` / `models` WS-RPC so no new daemon→server channel is needed. Add copilot and kimi to `DYNAMIC_RUNTIMES` once the probe is built | ⬜ Pending (daemon→server channel now exists; only the ACP probe subprocess remains) |
| I50 | **opencode `--verbose` `reasoning.variants` not consumed** | `listModels.ts` calls `opencode models --verbose` and parses the JSON; model names are extracted but the per-model `reasoning.variants` (thinking capability flags, analogous to claude `--effort` levels) are silently dropped. As a result, opencode models that support thinking are treated as plain text-only models in the UI | Parse `reasoning.variants` from the verbose JSON and include them in the returned model list as thinking-level descriptors, mirroring the claude/codex path | ⬜ Pending |
| I51 | **claude effort levels applied globally, not per-model** | `listModels.ts` probed `claude --help` once for the global `--effort` superset and attached it to *every* claude model, so Sonnet/Haiku advertised `xhigh` (Opus-only) and Haiku advertised `max` — selecting an unsupported level fails at agent runtime (`error` activity), defeating discovery's whole point | **Fixed**: `listModels.ts` now parses the `--effort` superset and **projects it through a per-model allow-list** (`CLAUDE_MODEL_EFFORT_ALLOW` — multica's `claudeModelEffortAllow` collapsed to our short ids: opus=low/med/high/xhigh/max, sonnet=low/med/high/max, haiku=low/med/high) + friendly labels (`xhigh`→"Extra high"). Result is `superset ∩ allow-list`, so a CLI bump still narrows correctly. Unit-tested (6 cases in `listModels.unit.test.ts`); live-verified on an isolated stack (sonnet no longer offers xhigh, haiku drops xhigh+max). **Caveat — the allow-list is hand-maintained**: a new Anthropic model absent from the map keeps the full superset (defensive) until the table is updated. | ✅ Fixed (this PR) |
| I52 | **First runtime-models probe (up to ~8s timeout) has no loading indicator in the create-agent modal** | `runtimeModels.ts` fires a `probe-models` WS-RPC on cache miss; the timeout is intentionally generous (~8s) to survive a slow shell spawn. During that window the model dropdown in the create-agent modal is populated with a static fallback list (or empty), with no spinner/skeleton. A user who picks a model before the probe resolves gets the stale static list | Show a loading state in the model dropdown while a probe is in flight; swap to the discovered list when it resolves, preserving any selection if the model still appears in the new list | ⬜ Pending |
| I53 | **`listModels` inherits daemon `process.env` minus `NODE_OPTIONS`; agent-specific env not injected** | `listModels.ts` spawns runtime binaries with the daemon's own environment (`process.env`, stripping `NODE_OPTIONS` to match the per-runtime pattern in the runtime adapters). It does **not** inject any per-agent env (e.g. a custom `OPENAI_API_BASE` or provider key stored per agent). Some runtimes that read config from env rather than a config file (e.g. an opencode setup pointing at a non-default provider) may therefore enumerate a different — possibly empty — model list than what the actual agent session would see | Decide whether `probe-models` should carry an `agentId` so `listModels` can look up that agent's runtime config and inject it; or document that model discovery uses the daemon's ambient env and agents relying on per-agent provider overrides may see partial lists | ⬜ Pending |
| I40 | **Landing reset defeats per-element `margin-top` on `<h1>/<h2>/<h3>/<p>`** | `landing.css` `.lp-root h1,h2,h3,p { margin: 0 }` has specificity (0,1,1), which beats single-class rules like `.lp-hero__sub { margin-top: var(--lp-space-8) }` (0,1,0) → those margins silently collapse to 0. Caused the hero `<h1>` title and `<p>` subtitle to overlap (verified: text-ink gap was −2px). DIV/SPAN elements (e.g. `.lp-hero__actions`) are unaffected, which is why only the text elements collided | **Hero fixed** by scoping its title/sub/note under `.lp-hero__intro` (0,2,0, beats the reset; gap restored to 30px, browser-verified). **Residual:** `.lp-section-lead` and `.lp-engine__desc` `margin-top` are still defeated by the same reset — audit and either scope them too or change the reset to `:where(.lp-root) :where(h1,h2,h3,p)` (0 specificity) after checking it doesn't double-space the rest of the page | 🟡 hero resolved; section-lead / engine-desc to audit |
| I42 | **`activityDetail` can show a stale tool string after an activity transition** | `store.tsx` `agent:activity` handler keeps the old detail when a new event carries none: `activityDetail: p.detail ?? a.activityDetail`. When an agent goes `working`("Running command X") → `thinking`(no detail), the old "Running command X" lingers. Pre-existing store logic, **not introduced by the live-agent-bar PR**, but that bar (`LiveAgentBar.tsx labelOf`) is the first UI to surface `activityDetail` prominently and persistently, so it amplifies the staleness | Decide the intended semantic: either clear detail on a detail-less event (`p.detail ?? null`) — affects the per-DM dot tooltip too, so verify both consumers — or have producers always send a detail. Keep the `?? a.activityDetail` for now (changing it is its own cross-consumer change); `labelOf` already falls back to a generic "Working/Thinking" label when detail is empty | ⬜ Pending (pre-existing; surfaced by code-review of live-agent-bar) |
| I41 | **Runtime adapters — deferred hardening (low priority)** | Code-review of the 5 one-shot runtime adapters (copilot/opencode/kimi/pi/cursor) identified a set of improvements that are correct-but-not-urgent: exit-75 transient retry (kimi), content-as-array support (kimi multi-modal), token-usage capture (all), MCP server injection (all), cursor thinking-block trajectory kind, pi `auto_retry_end` signal, errTail pollution by tool stdout mixed with CLI errors, unbounded `buf` on pathological output (no `MAX_BUF_BYTES` guard), `writeFileSync` → async fs.writeFile (avoids main-thread block on slow network FS), `onExit(null)` → `onExit(1)` coercion (signal-killed processes emit `null`) | Address per-runtime as follow-up slices when the relevant CLIs stabilize | ⬜ Pending (post-v1; none of these affect the happy path, only edge cases) |
| I43 | **`POST`/`DELETE /servers/:id/machines` lack a `manageMachines` capability check** — any workspace member (not just owner/admin) can create or delete machines | `routes-api.ts`: the create + delete machine endpoints gate only on server membership (the ~line 227 `not a member` check), never `requireCap(…, "manageMachines")`, even though that capability exists (`capabilities.ts`, owner/admin-only) and the frontend already hides machine management from members. The new `POST …/machines/:id/reconnect` (this PR) **does** enforce `manageMachines`, establishing the pattern — create/delete still need it for parity | Add `if (!await requireCap(serverId, userId, "manageMachines")) return 403` to the create + delete machine endpoints | ✅ Fixed — both endpoints now enforce `manageMachines`; owner-create regression-verified (200) in the authorization-hardening PR |
| I44 | **Authorization layer — systemic access-control gaps (two-plane security audit)** | A human-plane (`routes-api`/`capabilities`) + agent-plane (`routes-agent`/`scopes`/`core.ts resolveTarget`) audit found ~20 gaps in two families: (a) cross-tenant IDOR — `:id` lookups not scoped by the auth-derived `serverId`; (b) missing resource-access checks — scope/role pass but no channel-membership/ownership/capability check on the specific resource. Canonical model + full prioritized register: **`docs/authorization.md`** | Slices shipped: (1) cross-tenant IDOR batch F4/F6/F7/F9/F10 + agent C9 + machine F1/F2 `manageMachines` + C11; (2) **agent-plane channel-access layer C1/C2/C3/C6/C7/C8 + server/info** via `canAgentReadChannel`; (3) **human capability gates F3/F5/F8** (`manageAgents`/`manageChannels` + channel-ownership pre-check; real E2E member→403/owner→200) **+ C4** deleted-agent token (`resolveAgent` `isNull(deletedAt)` + clear hash on soft-delete; E2E token rejected after delete). **Remaining tracked in `docs/authorization.md` §6**, each its own PR: task-ownership C5; auth primitives C10 (timing-safe compare)/C12 (token TTL) | 🟢 3 slices done; only C5/C10/C12 remain (`docs/authorization.md` §6) |
| I45 | **Reconnect catch-up: a hand-kept cross-file mirror + an out-of-scope case + 3 logic-only verifications** | `reconnectCatchup.ts` (+ `core.ts` createMessage wake branch) | (a) `isWakeable` is a manual mirror of `core.ts createMessage`'s wake branch — the two must change together or reconnect-wake drifts from online-wake (the unit test locks the criterion shape, not the cross-file equivalence). (b) Agents with `machineId = null` (e.g. seed-created, never bound to a machine) are out of scope — no machine-reconnect event fires for them; recovery falls back to the next @. (c) Not separately E2E'd (all share the verified `onReady` path): the soft-offline `agent:deliver` path (WS dropped but process alive), the 30s per-machine cooldown guard (`OPEN_TAG_CATCHUP_COOLDOWN_MS`), and the server-restart trigger | (a) consider one shared `wouldWake(agent, msg)` consumed by both sites; (b) decide whether machineId-null agents should be swept on any reconnect or stay @-driven; (c) add targeted E2E if a path proves fragile | ⬜ Pending (none affect the verified happy path) |
| I46 | **Member-profile "Created Agents" card leaked soft-deleted agents** (+ a note on the dup-name race window) | The profile endpoint `GET /servers/:sid/members/:uid/profile` (`routes-api.ts`) queried `agents` by `(serverId, creatorId)` **without** `isNull(deletedAt)`, unlike every other list path (`GET /api/agents`, `core.workspaceMembers`, agent-api `server/info`), so a soft-deleted agent stayed visible in the human-profile card — its status dot reads straight from `agents.status`, which soft-delete sets to `inactive`. Surfaced with I26: the user's visible duplicate `codex-codereview` pair was a deleted-then-recreated same-name pair leaking through this hole | Added `isNull(schema.agents.deletedAt)` to the profile query, matching the global list. **Race-proofed (this session):** dup-name rejection uses `onConflictDoNothing` against the partial index (single path, no SELECT-then-INSERT gap) → a duplicate live name returns 409 with no 500 race window (verified: 8 concurrent same-name POSTs → 1×200 / 7×409 / 0×500, one live row) | ✅ Fixed (profile leak, curl-verified delete→card empty; dup-name guard race-proofed via onConflict — 0×500 under concurrency) |

| I34 | **The `target=` shown to agents was unresolvable for thread-anchor messages** → reply silently misfired/404 | `fmt` (`src/server/routes-agent.ts`) rendered a message with `m.threadId` (a task/thread-anchor) as `#chan:<threadChannelId8>`, but `resolveTarget` (`core.ts`) parses the `:suffix` as a **parent message id** prefix (`messages.id LIKE suffix%`). A thread *channel* id never matches a *message* id → `null` → `404 TARGET_FAILED`. Since the standing prompt tells agents to "reuse the `target=` value", reusing the shown target for any threaded message failed — observed live (an agent's replies landed in a task thread / `#chan:<threadId>` reads 404'd) and surfaced during task #3 bug-hunt | **Fixed**: `fmt` now emits `#chan:<m.id8>` (the anchor message's own id = the thread's parent), matching `addressableTarget`'s convention so the shown target round-trips through `resolveTarget`. Regression test `test/threadTargetRoundtrip.integration.ts` (real DB: task message's shown target resolves to its thread; plain message resolves to its channel) — RED before fix, GREEN after; existing `threadTarget.integration.ts` still passes. Related to I30 (thread/target resolution). **Not yet live-verified** (would require restarting the running server, which would disrupt active agents) | ✅ Done (task #3) |
| I47 | **Two conflicting `.seg` rules in `styles.css`** — `.seg` is defined twice: one pill-shaped (`border-radius:9999px`, `.seg-opt` children, for the landing/language picker) and one rounded-rect (`border-radius:8px`, `button` children, for the task toolbar). Same specificity → the later (8px) rule wins for *every* `.seg`, so the pill variant's `9999px` is silently clobbered. Pre-existing; surfaced by code-review of the board-layout-toggle PR, which added a `.seg-icon` modifier on the 8px (task-toolbar) variant | `web/src/styles.css` two `.seg{…}` blocks (`border-radius:9999px` vs `:8px`); new `.seg-icon` correctly extends the 8px one but inherits the ambiguity | Consolidate to one base `.seg` + explicit variant classes (e.g. `.seg-pill` / `.seg-rect`) so a future `.seg` consumer isn't surprised by cascade order. **Not fixed here** (out of scope for the layout-toggle PR; surgical-change discipline — don't refactor adjacent pre-existing CSS) | ⬜ Pending (pre-existing; low priority, cosmetic) |
| I48 | **npm package `@fancyboi999/open-tag-daemon` lagged `main` → prod ran a daemon with no `copilot` runtime** (`offline · no runtime: copilot`). `0.1.0` was published 2026-06-24 19:45, *before* #44 (copilot/opencode/kimi/pi/cursor, merged 23:57) — and #45–#54's daemon changes never shipped either. Prod/self-host machines run `npx @fancyboi999/open-tag-daemon`, so merging to `main` never reached them; CI was green and docs synced, masking it. | `packages/daemon/package.json` (was `0.1.0`); `publish-daemon.yml` fires only on a *published GitHub Release*; nothing flagged "daemon change ⇒ cut a release". Also: npm **Trusted Publisher was never actually registered** (I33 marked OIDC "Done" but it had never published — first OIDC run E404'd). | Added a **Release discipline** section + doc-sync row in `AGENTS.md` (merged ≠ shipped) + this entry; bumped `packages/daemon` → `0.2.0`, cut the `v0.2.0` Release. Registered the npm Trusted Publisher (owner `fancyboi999` / repo `open-tag` / workflow `publish-daemon.yml`), reran the workflow → OIDC publish succeeded. Restarted mymac's daemon on `npx …@0.2.0`. | ✅ Fixed (v0.2.0 live on npm via OIDC; mymac daemon @0.2.0 ready with 7 runtimes incl. `copilot`; `copilot-dev-codereview` started `runtime=copilot`, **0× "no runtime"** in the new daemon log) |
| I49 | **`daemonVersion` was hard-coded `"0.1.0"` in `src/daemon/index.ts`** → every machine reported a fake version (mymac showed `0.1.0` while running the 0.2.0 bundle with all 7 runtimes) | `index.ts:59` put a literal `"0.1.0"` in the `ready` payload regardless of the published package version; surfaced while verifying I48 (runtimes updated to include copilot but the version field stayed 0.1.0) | `index.ts` now reads `process.env.DAEMON_VERSION ?? "dev"`; `build-daemon-pkg.mjs` injects the real `packages/daemon/package.json` version via esbuild `define` at bundle time (verified: `dist/cli.mjs` compiles `daemonVersion: "0.2.1"`); source/tsx runs report `"dev"` | ✅ Fixed (bundle inlines the package version; typecheck green; ships in 0.2.1) |
| I51 | **Per-runtime skills dirs: kimi + project-local (workspace) dirs are best-effort** | `skillRootsFor` (`src/daemon/workspace.ts`) ports multica's `localSkillRootsForProvider` for the **home** provider dirs (claude/codex/copilot/opencode/cursor/pi — authoritative from multica) + universal `~/.agents/skills`. Two soft spots: (a) **kimi** has no established skills dir, so it falls back to the universal dir only — if Kimi Code ships a skills convention, add it; (b) the **workspace** (cwd-relative) provider dir mirrors the home leaf (`.claude` / `.codex` / `.opencode` …), which is verified only for claude (`.claude/skills`); for other runtimes a wrong project-local path just yields an empty scan (harmless), not a load of the wrong dir | Confirm kimi's skills path and each runtime's project-local skills convention; until then the home provider dir (the one agents actually load, since they run with the real `$HOME`) is correct and unit-tested (`test/skillRoots.unit.test.ts`) | ⬜ Pending (low priority; home dirs correct, workspace dirs harmless-if-wrong) |
| I50 | **Empty states unified into a two-tier system** | A shared `<PaneEmpty>` (`web/src/PaneEmpty.tsx` — icon + EB Garamond serif headline + optional `--muted` sub-line, vertically centered) now backs every **primary full content-pane** empty: Inbox, Saved, the Tasks board, an empty channel (`Chat.tsx`, gated on a `loaded` flag so it never flashes mid-fetch), and the no-machine onboarding hint. **Secondary sub-section** empties that sit under a section header inside a profile/detail tab (Skills / Apps / DMs / Reminders / Workspace / Activity, plus channel Files) deliberately keep the inline `.empty` — a giant serif headline under a "DMs" header (or in the narrow `traj-col` profile panel) would over-dramatize a minor state — but `.empty` (and `.sel-empty` / `.msg-sys`) now use `--muted` (clears WCAG AA on the off-white canvas; the prior `--muted-soft` ≈2.4:1 failed). Tertiary metadata (counts, timestamps, file sizes, sub-eyebrows) stays `--muted-soft` by design. **Search "no results" stays inline** (renders below a persistent search input; centering would float it far from the box). | n/a — the remaining inline empties are an intentional altitude choice, not pending work | ✅ Done |
| I51 | **`TaskBoard.tsx` defines `Card` / `StatusPill` / `DraggableCard` / `DroppableCol` INSIDE the component** → new function identities every render → React **remounts** them on any `TaskBoard` state change. Costs: cards lose DOM/transition continuity, dnd-kit re-registers each draggable/droppable per render, and component-local state is lost (this is why the status menu's open state had to be **hoisted to `TaskBoard` + portaled** in the board-move-ux PR rather than living in `StatusPill`). | `web/src/TaskBoard.tsx` (the four inner components); surfaced while root-causing a "click the status pill, menu won't open until you move the mouse" bug — the menu flag was being reset by remounts (`MeasuringStrategy.Always` amplified the churn, since removed). | **Proper fix:** extract `Card`/`StatusPill`/`DraggableCard`/`DroppableCol` to module scope (or a `TaskCard.tsx`), passing the handful of needed props/handlers, so they stop remounting. The move-ux PR neutralized the acute symptom (hoisted menu state, dropped `Always`) but left the components inline. | ⬜ Pending (workaround in place; extraction is the real fix) |
| I52 | **`.task-col-body{overflow-y:auto}` makes each Kanban column its own scroll container** — fine today (modest card counts, no overflow), but on a column with many cards dnd-kit's auto-scroll may scroll a non-target column near a drag edge. The droppable is the full-height `.task-col` (not the scrolled body), so drop-target *measuring* is unaffected; only auto-scroll behaviour is the open question. | `web/src/styles.css` `.task-col-body`; `DndContext` has no `autoScroll` override. | If a many-card column ever shows scroll-jump during drag, configure `DndContext autoScroll` (or scope the scroll container). Not worth pre-building. | ⬜ Pending (edge-case; no repro on current boards) |

> Maintenance: consider a scheduled doc-gardening agent (or run project `/doc-sync` skill) to periodically scan this table against code, auto-append new drift, and open PRs for fixes.
